import { Custom2Directive } from './custom-2.directive';

describe('Custom2Directive', () => {
  it('should create an instance', () => {
    const directive = new Custom2Directive();
    expect(directive).toBeTruthy();
  });
});
